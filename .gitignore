*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/build/
build/

# Release signing — NEVER commit (secrets)
keystore.properties
*.jks
*.keystore

# Local release staging (large signed APKs — hosted at rcq.app/android/, not in git)
release-v*/

# Wrangler local cache: holds the Cloudflare account id and name of
# whoever ran it. Never source.
.wrangler/
